"""Core engine for oaibatch.

These modules are framework-internal. Users should consume the public API
through `oaibatch.Run` and the `oaibatch.BatchTask` Protocol, not by
importing from `oaibatch.core` directly.
"""

"""Core engine for oai-batchkit.

These modules are framework-internal. Users should consume the public API
through `oai_batchkit.Run` and the `oai_batchkit.BatchTask` Protocol, not by
importing from `oai_batchkit.core` directly.
"""
